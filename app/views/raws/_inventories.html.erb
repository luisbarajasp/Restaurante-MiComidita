<ul class="list-group list-group-flush">
    <% @raw_inventories.each do |inventory| %>
        <li class="list-group-item">
                <%= pluralize( "%g" % inventory.quantity, @raw.measure ) %> |
                <%= inventory.expired_at.to_time.strftime('%m/%d/%Y') %>                    
            </div>
        </li>
    <% end %>
</ul>